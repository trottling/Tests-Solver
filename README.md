# VK Test Solver Bot (Go)

Бот для VK, который принимает картинку с тестом, отправляет её в OpenAI и стримит ответ с обновлением сообщения каждые 3 секунды.

## Возможности

- Приём фото как `photo` или `doc` (файл-картинка).
- Параметры запроса - детализация картинки `detail=low`, `reasoning_effort=low`, `max_completion_tokens=12000`.
- Стриминг результата с редактированием одного VK-сообщения раз в 3 секунды.
- Middleware:
  - access-list по `user_id`;
  - ограничение количества одновременных задач на пользователя.
- Пул воркеров для одновременной обработки нескольких запросов.
- Ключи/параметры грузятся из YAML-конфига, включая OpenAI `base_url`.

## Запуск

```bash
go mod tidy
go run ./cmd/bot -config config.yaml
```

## Конфиг

См. `config.example.yaml`.
